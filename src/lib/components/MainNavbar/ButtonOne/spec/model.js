import R from 'res/R';

const model = {
  buttons: {
    backButton: {
      handlePress: () => {},
      src: R.images.backButton,
    },
    logoButton: {
      handlePress: () => {},
      src: R.images.logoButton,
    },
  },
};

export default model;
